FROM docker.io/library/python:3.12-slim
ARG AGENT_VERSION=0
ENV AGENT_VERSION=${AGENT_VERSION}
RUN useradd -u 10001 -m agentrun
COPY app.py /app/app.py
USER agentrun
EXPOSE 8080
ENTRYPOINT ["python3", "/app/app.py"]
